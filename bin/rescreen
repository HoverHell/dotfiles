#!/bin/bash
#screen -x screenterm || screen -c ~/.screenrc.term -S screenterm
echo "export DISPLAY=$DISPLAY" > ~/disp
if [ -x ~/bin/rescreen.local ]; then
  exec ~/bin/rescreen.local "$@"
  exit $?
else
  exec ~/bin/mrescreen
  exit $?
fi
