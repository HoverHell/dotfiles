#!/bin/sh
## Repetitive ssh-rescreen
if [ x"$1" = x"-x" ]; then
    screenx=1
    shift 1
fi

if [ $screenx ]; then
    sargs="-x"
else
    sargs="-D -R"
fi

exec _s "$@"  -t "bin/rescreen || screen $sargs -S happs"  # `-X` is not completely safe
