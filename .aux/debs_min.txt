# Minimal amount of debian packages to not bother with “oh look, that thing isn't installed”.
# Usage:
# apt-get update && apt-get --yes install $(sed '/^#/ d' debs_min.txt)
# Alternatively:
# apt-get update && apt-get --yes install aptitude && aptitude update && aptitude upgrade
# aptitude --visual-preview install $(sed '/^#/ d' ~/src/dotfiles/.aux/debs_min.txt)

# Best-hope for when stuff gets a little complicated.
aptitude

# # # often-used tools:
# # local tools
screen jq bc sqlite3 mc p7zip-full unp xclip
# # dev-ish tools:
devscripts moreutils rename wdiff colordiff strace time wipe uuid gdebi-core
# # remote tools:
git curl wget rsync socat nmap bind9-host openssh-client mosh tsocks sshfs
lynx w3m youtube-dl
apt-transport-https

# # system monitoring:
iproute2 iftop iotop htop lshw lsof powertop smartmontools
openssh-server etckeeper
# # likely to be pre-installed though
bash bash-completion less diffutils gnupg grep

# build environment
build-essential cmake autoconf automake make shellcheck pkg-config llvm
# some bunch of build dependencies
libc6-dev libffi-dev
libncurses5-dev libncursesw5-dev libgdbm-dev libreadline-dev tk-dev
libsqlite3-dev libssl-dev openssl
libcap-dev libcurl4-nss-dev libpq-dev libfreetype6-dev libpng-dev
zlib1g-dev libbz2-dev libzip-dev liblzma-dev xz-utils
libxml2-dev libxmlsec1-dev
liblapack-dev libblas-dev
libboost-all-dev lua5.2 liblua5.2-dev libtbb-dev

# ...
python python-dev python3 python3-dev
# python-setuptools python-pip python-smbus

# Just editors
joe
zile
emacs25
