#!/bin/sh -x

set -eu

if [ -z "$1" ]; then
    # exec firefox --no-remote -ProfileManager "$@"
    exit 1
else
    # exec firefox --no-remote -P "$@"
    mkdir -p ~/.jails/"$1"
    exec ~/.local/bin/_firejail_ff "$@"
fi
